When the user message contains a spreadsheet attachment (.xlsx, .xls, .csv) — even when the question is about the meaning of cells on a specific sheet ("why does X differ from Y", "explain this calculation", "compare these two values"), or any open-ended analysis question about the spreadsheet — automatically load the spreadsheet-extract skill and run the helper at /opt/data/skills/data-analysis/spreadsheet-extract/scripts/extract.py against /opt/uploads/ to read the whole file. Never answer from the RAG citation alone; it usually omits whole sheets.