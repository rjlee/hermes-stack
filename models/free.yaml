name: free
description: Free tier model via OpenRouter (elephant-alpha)
model:
  provider: openrouter
  default: "openrouter/elephant-alpha"
  cheap_model: "openrouter/elephant-alpha"
smart_model_routing:
  enabled: true
fallback_providers:
  - provider: openrouter
    default: nvidia/nemotron-3-nano-30b-a3b:free
embedding:
  provider: openrouter
  model: "nvidia/llama-nemotron-embed-vl-1b-v2"
tts:
  provider: edge
stt:
  provider: faster-whisper
  local:
    model: base